{
  "name": "casais-fiori-demo",
  "version": "0.1.0",
  "private": true,
  "description": "SAP BTP CAP/Fiori demo for CASAIS Fleet Intelligence integration.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "build": "cds build --production",
    "deploy:sqlite": "cds deploy --to sqlite",
    "test": "cds compile srv --to edmx > .tmp-equipment-service.edmx"
  },
  "dependencies": {
    "@cap-js/hana": "^1",
    "@cap-js/sqlite": "^2.4.0",
    "@sap/cds": "^9.3.1",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@sap/cds-dk": "^9.9.2"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite"
      },
      "[production]": {
        "db": {
          "kind": "hana"
        }
      }
    },
    "odata": {
      "version": "v4"
    }
  }
}
