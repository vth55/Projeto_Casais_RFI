<mvc:View
    controllerName="casais.equipment.controller.List"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:t="sap.ui.table"
    displayBlock="true">

    <f:DynamicPage id="listPage" fitContent="true" preserveHeaderStateOnScroll="true">
        <f:title>
            <f:DynamicPageTitle>
                <f:heading>
                    <Title text="Equipment" level="H1"/>
                </f:heading>
                <f:snappedContent>
                    <Text text="{= ${/Equipments/$count} + ' items'}"/>
                </f:snappedContent>
                <f:actions>
                    <Button
                        icon="sap-icon://refresh"
                        tooltip="Refresh"
                        press=".onRefresh"/>
                </f:actions>
            </f:DynamicPageTitle>
        </f:title>

        <f:content>
            <Table
                id="equipmentTable"
                items="{
                    path: '/Equipments',
                    parameters: {
                        $orderby: 'equipmentCode',
                        $count: true
                    }
                }"
                growing="true"
                growingThreshold="20"
                selectionChange=".onSelectionChange"
                mode="SingleSelectMaster"
                sticky="ColumnHeaders,HeaderToolbar">

                <headerToolbar>
                    <OverflowToolbar>
                        <Title id="tableTitle" text="Equipment" level="H2"/>
                        <ToolbarSpacer/>
                        <SearchField
                            width="20rem"
                            liveChange=".onSearch"
                            placeholder="Pesquisar código, equipamento ou projeto..."/>
                    </OverflowToolbar>
                </headerToolbar>

                <columns>
                    <Column width="8rem"><Text text="Code"/></Column>
                    <Column minScreenWidth="Tablet" demandPopin="true"><Text text="Equipamento"/></Column>
                    <Column width="10rem" minScreenWidth="Desktop" demandPopin="true"><Text text="Marca / Modelo"/></Column>
                    <Column width="10rem"><Text text="Status"/></Column>
                    <Column minScreenWidth="Tablet" demandPopin="true"><Text text="Projeto"/></Column>
                    <Column minScreenWidth="Desktop" demandPopin="true"><Text text="Localização"/></Column>
                    <Column width="10rem" minScreenWidth="Desktop" demandPopin="true"><Text text="N.º Ativo SAP"/></Column>
                </columns>

                <items>
                    <ColumnListItem type="Navigation" press=".onItemPress">
                        <cells>
                            <ObjectIdentifier
                                title="{equipmentCode}"
                                text="{= ${rfidTag} ? ${rfidTag} : '— sem tag NFC'}"/>
                            <Text text="{name}"/>
                            <Text text="{= ${brand} ? (${brand} + ' ' + (${model} || '')) : '—'}"/>
                            <ObjectStatus
                                text="{status}"
                                state="{= ${status} === 'AVAILABLE' ? 'Success' :
                                         ${status} === 'IN_USE'    ? 'Information' :
                                         ${status} === 'MAINTENANCE' ? 'Warning' : 'None' }"/>
                            <Text text="{assignedProject}"/>
                            <Text text="{currentLocationName}"/>
                            <Text text="{sapAssetNumber}"/>
                        </cells>
                    </ColumnListItem>
                </items>

                <noData><IllustratedMessage
                    illustrationType="sapIllus-EmptyList"
                    title="No equipment found"
                    description="Try adjusting your search criteria"/></noData>
            </Table>
        </f:content>
    </f:DynamicPage>
</mvc:View>
