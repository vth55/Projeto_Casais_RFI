# 🛠️ TAREFA EXECUTOR (CLAUDE CODE)

## CONTEXTO
Corrigir valores hardcoded no DashboardView para garantir que o sistema respeita as configurações dinâmicas da Sede.

## CHECKLIST DE EXECUÇÃO

### 1. Bug: CO2 Hardcoded (dashboard:co2)
- [x] **Implementar**: No `DashboardView.jsx` (L993), substituir o valor fixo `2.68` por `systemSettings.co2FactorPerLitre`. ✅ DONE
- [ ] **Testar**: Abrir **Configurações**, alterar o fator CO2, e confirmar que o gráfico no **Dashboard** reflete a mudança.

### 2. Bug: Alertas de Manutenção (dashboard:maintenance)
- [x] **Implementar**: No `DashboardView.jsx` (L1029), substituir o valor fixar `>= 120` por uma lógica que verifique:
    1. `machine.maintenanceInterval` (se existir override na máquina)
    2. Caso contrário, usar `systemSettings.defaultMaintenanceInterval` (valor global).
    Threshold a 80% do intervalo (equivalente ao 120/150 anterior). ✅ DONE
- [ ] **Testar**: Alterar o intervalo de manutenção global nas **Configurações** e validar se os alertas no Dashboard (ou widget de foco) são atualizados.

---

## ESTRATÉGIA DE TOKENS
- Focar apenas nos patches cirúrgicos indicados.
- Não refatorar outros componentes sem autorização.
- Após concluir, responder apenas confirmando a execução dos testes e o estado final.
